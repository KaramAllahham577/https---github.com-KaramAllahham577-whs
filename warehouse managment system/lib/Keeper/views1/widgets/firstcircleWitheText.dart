import 'package:flutter/material.dart';


class FirstCircleWithText extends StatelessWidget {
  const FirstCircleWithText({super.key});

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [
      ],
    );
  }
}
