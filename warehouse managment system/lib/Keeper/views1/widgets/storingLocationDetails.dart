import 'package:flutter/material.dart';

class StoringLocationDetails extends StatelessWidget {
  const StoringLocationDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
